{{- if .Values.RoleBinding }}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "helm.fullname" . }}
  # namespace: {{ .Release.Namespace }}
{{- toYaml .Values.RoleBinding | nindent 0 }}
{{- end }}

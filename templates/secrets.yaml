{{- if .Values.secrets }}
{{- range .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .name }}
  annotations:
    {{- toYaml .annotations | nindent 8 }}
type: {{- toYaml .type | nindent 0 }}
data:
  {{- toYaml .data | nindent 2 }}
---
{{- end }}
{{- end }}